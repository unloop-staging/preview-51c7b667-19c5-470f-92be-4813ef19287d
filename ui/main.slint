// Generated by Unloop - 2025-12-31T15:15:44.248Z
// Smart Coffee Maker: Control coffee brewing with customizable strength and scheduling

import { MaterialWindow } from "@material";
import { AppData } from "data.slint";
import { HomeScreen } from "screens.slint";
import { ControlScreen } from "screens.slint";
import { SettingsScreen } from "screens.slint";

export component App inherits MaterialWindow {
    title: "Smart Coffee Maker";
    width: 800px;
    height: 480px;
    background: #1E1E1E;
    
    in-out property <string> active-screen: "home";
    in-out property <AppData> data: {
        water-level: 85,
        bean-level: 65,
        brew-temp: 195,
        machine-status: "ok",
        filter-status: "ok",
        brew-strength: 5,
        target-strength: 5,
        default-strength: 5,
        brew-temp-setting: 195,
        auto-clean-enabled: true,
        version: "2.1.0",

    };
    
    callback brew-start-clicked();
    callback schedule-clicked();
    callback target-changed(float);
    callback mode-changed(string);
    
    VerticalLayout {
        Rectangle {
            vertical-stretch: 1;
            
            if active-screen == "home" : HomeScreen {
                data <=> root.data;
            }

            if active-screen == "control" : ControlScreen {
                data <=> root.data;
            }

            if active-screen == "settings" : SettingsScreen {
                data <=> root.data;
            }

        }
        
        Rectangle {
            height: 80px;
            background: #2A2A2A;
            
            HorizontalLayout {
                alignment: space-evenly;
                
                Rectangle {
                    horizontal-stretch: 1;
                    
                    TouchArea {
                        clicked => { active-screen = "home"; }
                    }
                    
                    VerticalLayout {
                        alignment: center;
                        spacing: 4px;
                        
                        Rectangle {
                            width: 24px;
                            height: 24px;
                        }
                        
                        Text {
                            text: "Home";
                            font-size: 12px;
                            color: active-screen == "home" ? #3AA1F5 : #9E9E9E;
                            horizontal-alignment: center;
                        }
                    }
                }
                Rectangle {
                    horizontal-stretch: 1;
                    
                    TouchArea {
                        clicked => { active-screen = "control"; }
                    }
                    
                    VerticalLayout {
                        alignment: center;
                        spacing: 4px;
                        
                        Rectangle {
                            width: 24px;
                            height: 24px;
                        }
                        
                        Text {
                            text: "Brew";
                            font-size: 12px;
                            color: active-screen == "control" ? #3AA1F5 : #9E9E9E;
                            horizontal-alignment: center;
                        }
                    }
                }
                Rectangle {
                    horizontal-stretch: 1;
                    
                    TouchArea {
                        clicked => { active-screen = "settings"; }
                    }
                    
                    VerticalLayout {
                        alignment: center;
                        spacing: 4px;
                        
                        Rectangle {
                            width: 24px;
                            height: 24px;
                        }
                        
                        Text {
                            text: "Settings";
                            font-size: 12px;
                            color: active-screen == "settings" ? #3AA1F5 : #9E9E9E;
                            horizontal-alignment: center;
                        }
                    }
                }
            }
        }
    }
}
